#pragma once

#include <vector>

namespace moth
{

typedef std::vector<unsigned int> Args;

enum FONT_SIZE
{
	FONT_SMALL,
	FONT_MEDIUM,
	FONT_LARGE
};

enum NEXT_LINE
{
	LINE_LOCATION,
	LINE_NODE,
	LINE_RESPONSE
};

enum MARGIN
{
	MARGIN_TOP,
	MARGIN_RIGHT,
	MARGIN_BOTTOM,
	MARGIN_LEFT
};

enum DISPLAY_AREA
{
	AREA_LEVELS,
	AREA_MAIN,
	AREA_BUTTONS
};

enum FLOW
{
	FLOW_VERTICAL,
	FLOW_HORIZONTAL
};

enum LEVEL
{
	LEVEL_HUNGER,
	LEVEL_HUMANITY
};

enum LAYER
{
	LAYER_DEFAULT,
	LAYER_JITTER,
	LAYER_PIXELLATED,
	LAYER_DISTORTED
};


enum RESPONSE
{
	RESPONSE_NORMAL,
	RESPONSE_SECRET,
	RESPONSE_TEACH
};

enum LOCATION
{
	LOCATION_NORMAL,
	LOCATION_MOTH,
	LOCATION_OBSTACLE
};

enum AUDIO
{
	AUDIO_BUTTON,
	AUDIO_GO,
	AUDIO_EAT,
	AUDIO_SECRET
};

}
